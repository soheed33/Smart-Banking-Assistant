# خلاصه‌ی نتایج ارزیابی

> بخش‌های علامت‌گذاری‌شده با ✅ از خروجی واقعی اجرای نوت‌بوک گرفته شده‌اند. بخش ۳ (RAGAS/LLM-as-Judge) هم واقعاً اجرا شد، اما به دلیل محدودیت کوتای روزانه‌ی سطح رایگان Google API، بدون نتیجه‌ی عددی معتبر باقی ماند (جزئیات در همان بخش).

## ۱. طبقه‌بندی نیت (Intent Classification) ✅

سیستم Hybrid (طبقه‌بند ParsBERT + LLM fallback)، روی ۱۳۰ نمونه‌ی تست از test_intent.json:

| متریک | مقدار |
|---|---|
| Accuracy | 0.8308 |
| Precision (macro) | 0.8474 |
| Precision (weighted) | 0.8621 |
| Recall (macro) | 0.8507 |
| Recall (weighted) | 0.8308 |
| F1 (macro) | 0.8270 |
| F1 (weighted) | 0.8310 |
| میانگین اطمینان (confidence) | 0.3223 |
| تعداد فراخوانی LLM fallback | 78 از 130 |

### گزارش دقیق به‌ازای هر نیت

| نیت | Precision | Recall | F1 | تعداد نمونه |
|---|---|---|---|---|
| block_card | 0.89 | 1.00 | 0.94 | 8 |
| buy_charge | 0.89 | 1.00 | 0.94 | 8 |
| buy_internet | 1.00 | 1.00 | 1.00 | 8 |
| card_balance | 0.60 | 0.86 | 0.71 | 7 |
| card_circulation | 0.45 | 0.71 | 0.56 | 7 |
| check_registration | 1.00 | 0.43 | 0.60 | 7 |
| external_bank_shaba_transfer | 0.75 | 0.86 | 0.80 | 7 |
| faq | 0.89 | 0.78 | 0.83 | 50 |
| pay_house_utility_bills | 1.00 | 1.00 | 1.00 | 7 |
| pay_installments | 0.70 | 1.00 | 0.82 | 7 |
| pay_phone_bills | 1.00 | 1.00 | 1.00 | 7 |
| transfer_card_to_card | 1.00 | 0.57 | 0.73 | 7 |

**تفسیر:** ضعیف‌ترین عملکرد مربوط به card_circulation (F1=0.56) و check_registration (F1=0.60، Recall پایین) است. buy_internet, pay_house_utility_bills و pay_phone_bills عملکرد کامل (F1=1.00) دارند. با توجه به اینکه 78 از 130 نمونه (60%) به LLM fallback ارجاع شده‌اند، بخش عمده‌ای از دقت نهایی مدیون مسیر LLM است، نه فقط طبقه‌بند ParsBERT فاین‌تیون‌شده.

## ۲. بازیابی RAG ✅

مجموعه‌ی تست: 150 پرسش FAQ + 20 پرسش خارج از حوزه (OOD) = 170 پرسش

### دقت و پوشش بازیابی (روی پرسش‌های FAQ)

| متریک | مقدار |
|---|---|
| Precision@1 | 0.4600 |
| Precision@3 | 0.2200 |
| Precision@5 | 0.1507 |
| Recall@1 | 0.4600 |
| Recall@3 | 0.6600 |
| Recall@5 | 0.7533 |
| F1@1 | 0.4600 |
| F1@3 | 0.3300 |
| F1@5 | 0.2511 |

### تشخیص خارج از حوزه (Out-of-Domain Detection)

| متریک | مقدار |
|---|---|
| OOD Accuracy | 0.9529 |
| OOD Precision | 1.0000 |
| OOD Recall | 0.6000 |
| OOD F1 | 0.7500 |

**تفسیر:** با افزایش k، Recall بالا می‌رود اما Precision و F1 کاهش می‌یابد. Precision کامل در تشخیص OOD (1.00) یعنی صفر مثبت کاذب، اما Recall پایین‌تر (0.60) نشان می‌دهد بخشی از پرسش‌های واقعاً خارج از حوزه به‌اشتباه طبقه‌بندی شده‌اند.

## ۳. ارزیابی RAGAS / LLM-as-Judge ⚠️ اجرا شد، بدون داده‌ی معتبر

این ارزیابی روی نمونه‌ی 20تایی اجرا شد، اما **هیچ نتیجه‌ی قابل‌استفاده‌ای تولید نکرد** — نه به دلیل باگ در کد، بلکه به دلیل محدودیت سطح رایگان Google API.

| متریک | مقدار |
|---|---|
| Faithfulness | NaN (0 نمونه‌ی معتبر) |
| Context Precision | NaN (0 نمونه‌ی معتبر) |
| Answer Relevancy | NaN (0 نمونه‌ی معتبر) |
| همبستگی RAGAS ↔ LLM-as-Judge (Pearson/Spearman) | قابل‌محاسبه نبود (n=0 برای هر سه متریک) |

**علت ریشه‌ای:** سطح رایگان مدل `gemini-2.5-flash` محدود به **۲۰ درخواست در روز** است (`quotaId: GenerateRequestsPerDayPerProjectPerModel-FreeTier`, `quotaValue: 20`). ارزیابی RAGAS به‌تنهایی برای ۲۰ نمونه به حداقل ۶۰ فراخوانی نیاز دارد (۳ متریک × ۲۰ نمونه)، که فوراً از سقف روزانه عبور می‌کند؛ در نتیجه تمام ۶۰ Job با `TimeoutError` مواجه شدند و میانگین‌ها `NaN` ثبت شدند. سپس مرحله‌ی LLM-as-Judge، که کوتای روزانه از قبل صفر شده بود، برای هر پرسش و هر معیار (context_relevance، faithfulness، answer_completeness) با خطای `429 RESOURCE_EXHAUSTED` مواجه شد.

**نتیجه‌گیری برای گزارش:** این بخش از ارزیابی از نظر پیاده‌سازی کامل است (کد به‌درستی اجرا، لاگ‌برداری و ذخیره‌سازی خروجی را انجام می‌دهد)، اما به دلیل محدودیت زیرساختی سطح رایگان API (نه محدودیت طراحی سیستم)، امکان تولید نتیجه‌ی عددی معتبر در بازه‌ی زمانی این پروژه فراهم نشد. برای اجرای موفق، به یکی از موارد زیر نیاز است: (۱) حساب API با پلن پرداختی/سطح بالاتر، (۲) اجرا در طول چند روز متوالی با نمونه‌ی بسیار کوچک‌تر (مثلاً ۳-۵ نمونه در روز)، یا (۳) استفاده از یک ارائه‌دهنده‌ی LLM دیگر با کوتای رایگان بالاتر.

## ۴. فایل‌های خروجی تولیدشده

| فایل | وضعیت |
|---|---|
| intent_metrics.json | تولید شد |
| test_predictions.jsonl | تولید شد (170 پیش‌بینی) |
| retrieval_metrics.json | تولید شده در اجرای دیگر |
| improved_test_predictions.jsonl | TODO |
| ragas_scores.jsonl | تولید شد (20 رکورد) — مقادیر NaN به دلیل کوتای روزانه |
| judge_scores.jsonl | تولید شد (20 رکورد) — بدون امتیاز معتبر به دلیل کوتای روزانه |

## ۵. نتایج شکار باگ (50 سناریوی چندنوبتی) ✅

سناریوها در دو فایل ذخیره شدند: manual_bug_hunt_results.jsonl (سناریو 1 تا 20) و manual_bug_hunt_results_extra.jsonl (سناریو 21 تا 50).

| دسته | موضوع | تعداد سناریو |
|---|---|---|
| A | تشخیص و مسیریابی نیت | 8 |
| B | استخراج و فرمت اسلات‌ها | 14 |
| C | تایید/رد/ابهام در مرحله‌ی confirming | 8 |
| D | بازیابی FAQ و context گفتگو | 6 |
| E | اعتبارسنجی داده و ورودی‌های نامعتبر/تزریقی | 9 |
| F | گفتگوهای چندنیتی/طولانی (استرس‌تست) | 5 |

### رفتارهای درست تایید‌شده
- تشخیص صحیح OOD حتی در سه پرسش پیاپی بدون خرابی session (سناریو 23)
- تشخیص صحیح تغییر نیت با پیام کوتاه («مسدود کن» وسط جمع‌آوری اسلات، سناریو 5)
- رد صحیح شماره کارت با طول نامعتبر در چند نوبت پیاپی، بدون پذیرش مقدار غلط (سناریو 24)
- اصلاح صحیح مقدار بعد از رد در مرحله‌ی تایید، با جایگزینی درست کارت جدید (سناریو 14، 45)
- مدیریت صریح پاسخ‌های مبهم («شاید»، «نمی‌دونم») با درخواست مجدد بله/خیر (سناریو 15، 35)
- خنثی‌سازی کامل تلاش‌های تزریق دستور در مقدار اسلات‌ها؛ متن تزریقی صرفاً به‌عنوان داده ذخیره شد (سناریو 20، 42)
- عدم کرش با ورودی خالی یا فقط اموجی (سناریو 43)
